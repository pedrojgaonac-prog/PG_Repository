{
  "name": "mis-gastos",
  "version": "1.0.0",
  "private": true,
  "description": "PWA para supervisar gastos mensuales a partir de un Excel.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "python3 -m http.server 8080"
  }
}
